import React from "react";
import s from "./Repositories.module.scss";

const Repositories: React.FC = () => {
    return (
        <>
            <div className={s.main_repository}></div>
        </>
    );
};

export default Repositories;
